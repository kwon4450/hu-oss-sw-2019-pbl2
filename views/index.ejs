<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-9">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div id="container" style="border: 3px solid gray;">
        <div style="font-size: 80px;font-weight:900 ;margin-top: 10%; text-align: center;">
            Open Chat
        </div>
      <form action="/login" method="POST" style="margin-top: 6%;">
        <div>
          <input name="username" type="text" placeholder="id" minlength="5" maxlength="20" id="input" style="text-align:center; font-size:20px; margin-left: 16%; margin-top: 140px;width: 400px">
        </div>
        <div>
          <input name="password" type="password" placeholder="password" minlength="8" maxlength="16" id="input" style="text-align:center; margin-left: 16%;margin-top: 30px;margin-bottom: 50px;font-size:20px;width:400px">
        </div>
        <div>
          <button type="submit" id="send" style="margin-left: 36%">sign in</button>
          <a href="/join"><button type="button" id="send">sign up</button></a>
        </div>
        <div style="color: red; font-weight: 600; margin-top: 5%; text-align: center;">
          <% if (loginError) { %>
          <%= loginError %>
          <% } %>
        </div>
        <div style="color: blue; font-weight: 600; margin-top: 5%; text-align: center;">
          <% if (joinSuccess) { %>
          <%= joinSuccess %>
          <% } %>
        </div>
      </form>
    </div>
  </body>
</html>